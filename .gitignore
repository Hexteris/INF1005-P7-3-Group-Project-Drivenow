# Sensitive config - never commit
db-config.ini
db-config.ini.template
gen-hash.php

# VS Code SFTP credentials
.vscode/sftp.json

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Uploaded images stay on server, don't overwrite via git
/uploads/*
!/uploads/.gitkeep
!/uploads/cars/
/uploads/cars/*
!/uploads/cars/*.jpg
!/uploads/cars/*.png
!/uploads/cars/*.webp

# Node (if ever used)
node_modules/

#Google Maps API key - never commit real key
.env
